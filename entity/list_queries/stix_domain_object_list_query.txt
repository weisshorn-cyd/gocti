query ($after: ID
       $filters: FilterGroup
       $first: Int
       $orderBy: StixDomainObjectsOrdering
       $orderMode: OrderingMode
       $pirId: ID
       $search: String
       $types: [String]) {
    stixDomainObjects (after: $after
                       filters: $filters
                       first: $first
                       orderBy: $orderBy
                       orderMode: $orderMode
                       pirId: $pirId
                       search: $search
                       types: $types) {
        edges {
            node {%s}
        }
        pageInfo {
            startCursor
            endCursor
            hasNextPage
            hasPreviousPage
            globalCount
        }
    }
}
